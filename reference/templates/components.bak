<!DOCTYPE html>
<html>
<head>
    <title>Component Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/styles.css') }}">
    <script src="https://cdn.plot.ly/plotly-latest.min.js"></script>
</head>
<body>
    <div class="container mt-5">
        <!-- Add navigation -->
        <nav aria-label="breadcrumb" class="mb-4">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ url_for('index_route') }}">Home</a></li>
                <li class="breadcrumb-item active" aria-current="page">Component {{ component.id }}</li>
            </ol>
        </nav>
        <h1>Component Details</h1>
        
        {% if component %}
            <!-- Debug info -->
            <div class="debug-info" style="display: none;">
                <p>Component ID: {{ component.id }}</p>
                <p>Size: {{ component.size }}</p>
                <p>DMRs: {{ component.dmrs }}</p>
                <p>Genes: {{ component.genes }}</p>
                <p>Bicliques: {{ component.bicliques|length }}</p>
                <p>Split genes: {{ component.split_genes|length }}</p>
                <p>Total edges: {{ component.total_edges }}</p>
            </div>
        {% endif %}
        
        {% if component %}
        <div class="card mb-4">
            <div class="card-header">
                <h2>Component {{ component.id }}</h2>
            </div>
            <div class="card-body">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Total Bicliques</th>
                            <th>Total DMRs</th>
                            <th>Total Genes</th>
                            <th>Total Edges</th>
                            <th>Split Genes</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>{{ component.bicliques|length }}</td>
                            <td>{{ component.dmrs }}</td>
                            <td>{{ component.genes }}</td>
                            <td>{{ component.total_edges }}</td>
                            <td>{{ component.split_genes|length }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
                {% if component.split_genes %}
                <div class="mt-4">
                    <h3>Split Genes</h3>
                    <div class="card">
                        <div class="card-body">
                            <ul class="list-unstyled">
                                {% for gene in component.split_genes %}
                                <li>
                                    <strong>{{ gene.gene_name }}</strong>
                                    {% if gene.description != "N/A" %}
                                    <br><small class="text-muted">{{ gene.description }}</small>
                                    {% endif %}
                                    <br><small>Found in bicliques: {{ gene.bicliques|join(', ') }}</small>
                                </li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                </div>
                {% endif %}

                {% if component.bicliques %}
                <div class="mt-4">
                    <h3>Bicliques</h3>
                    {% for biclique in component.bicliques %}
                    <div class="card mb-3">
                        <div class="card-header">
                            <h4>Biclique {{ loop.index }} ({{ biclique.size }})</h4>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-6">
                                    <h5>DMRs</h5>
                                    <ul class="list-unstyled">
                                        {% for dmr in biclique.details.dmrs %}
                                        <li>{{ dmr.id }} - Area: {{ dmr.area }}</li>
                                        {% endfor %}
                                    </ul>
                                </div>
                                <div class="col-md-6">
                                    <h5>Genes</h5>
                                    <div class="row">
                                        <div class="col-md-6">
                                            <h6>Regular Genes</h6>
                                            <ul class="list-unstyled">
                                                {% for gene in biclique.details.genes %}
                                                    {% if not gene.is_split %}
                                                    <li>{{ gene.name }}
                                                        {% if gene.description != "N/A" %}
                                                        <br><small class="text-muted">{{ gene.description }}</small>
                                                        {% endif %}
                                                    </li>
                                                    {% endif %}
                                                {% endfor %}
                                            </ul>
                                        </div>
                                        <div class="col-md-6">
                                            <h6>Split Genes</h6>
                                            <ul class="list-unstyled">
                                                {% for gene in biclique.details.genes %}
                                                    {% if gene.is_split %}
                                                    <li><strong class="text-primary">{{ gene.name }}</strong>
                                                        {% if gene.description != "N/A" %}
                                                        <br><small class="text-muted">{{ gene.description }}</small>
                                                        {% endif %}
                                                    </li>
                                                    {% endif %}
                                                {% endfor %}
                                            </ul>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    {% endfor %}
                </div>
                {% endif %}

                {% if component.plotly_graph %}
                <div class="mt-4">
                    <h3>Visualization</h3>
                    <div id="plotly-graph-{{ component.id }}" class="plotly-graph"></div>
                    <script>
                        document.addEventListener('DOMContentLoaded', function() {
                            var graphData = {{ component.plotly_graph|safe }};
                            Plotly.newPlot('plotly-graph-{{ component.id }}', graphData.data, graphData.layout);
                        });
                    </script>
                </div>
                {% endif %}

                {% if component.single_connection_genes %}
                <div class="mt-4">
                    <h3>Genes with Single DMR Connection</h3>
                    <div class="card">
                        <div class="card-body">
                            <ul class="list-unstyled">
                                {% for gene in component.single_connection_genes %}
                                <li>{{ gene.gene_name }} - Connected to DMR_{{ gene.dmr + 1 }}
                                    {% if gene.description != "N/A" %}
                                    <br><small class="text-muted">{{ gene.description }}</small>
                                    {% endif %}
                                </li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                </div>
                {% endif %}
            </div>
        </div>
        {% else %}
        <div class="alert alert-warning">No component data available.</div>
        {% endif %}
    </div>
</body>
</html>
