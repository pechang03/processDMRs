if (typeof window.DEBUG === 'undefined') {
    window.DEBUG = true;
}

if (typeof window.EXPECTED_DATA_KEYS === 'undefined') {
    window.EXPECTED_DATA_KEYS = [
        'stats',
        'coverage',
        'components'
    ];
}
